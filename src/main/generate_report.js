import { db } from '../db/prepare_data.js';
const _  = require('lodash');
const Excel = require('exceljs');

export const generateReport = (batch) => {
  return new Promise((resolve, reject) => {
    resolve(true);
  });
};
